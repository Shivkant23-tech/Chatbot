# AI Chat App

A simple chat application with:
- Frontend: polished web chat UI
- Backend: Flask REST API
- AI model: OpenAI if OPENAI_API_KEY is set, otherwise a local fallback agent

## Install

```bash
pip install -r requirements.txt
```

## Run

```bash
python app.py
```

Then open http://localhost:5000
